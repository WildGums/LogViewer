<catel:UserControl x:Class="LogViewer.Views.LogTabView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:catel="http://catel.codeplex.com"
             xmlns:views="clr-namespace:LogViewer.Views"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <catel:TabControl ItemsSource="{Binding LogTabsData}">
            <catel:TabControl.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding Name}"/>
                </DataTemplate>
            </catel:TabControl.ItemTemplate>
            <catel:TabControl.ContentTemplate>
                <DataTemplate>
                    <views:LogTabItemView/>
                </DataTemplate>
            </catel:TabControl.ContentTemplate>
        </catel:TabControl>
    </Grid>
</catel:UserControl>
